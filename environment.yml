name: rca-kpis
# Conda env just provides Python + pip; package deps live in pyproject.toml (single
# source of truth) and are installed via `pip install -e .` below.
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pip:
      - -e .
